# Copy this file to .env.local and fill in your real key
GEMINI_API_KEY=

# Optional: set only if you want to force a specific model.
# If unset, the server will auto-pick a supported model for your API key.
# GEMINI_MODEL=gemini-2.0-flash
